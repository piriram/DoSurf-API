# 검증 도구 전용 의존성. **배포에는 들어가지 않는다** —
# Dockerfile 은 requirements.txt 만 설치한다. 여기에 있는 것을 옮기지 말 것.
#
#   .venv/bin/pip install -r requirements-dev.txt
#
# copernicusmarine 은 xarray·zarr·dask·numpy·pandas 를 끌고 온다 (설치 후 .venv 약 540MB).
# Cloud Run 이미지에 들어가면 콜드스타트가 그만큼 느려지므로 분리한 것이다.
# 쓰는 곳: scripts/copernicus.py, scripts/model_compare.py --models cmems
copernicusmarine==2.4.*
